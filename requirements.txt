# CPU-only PyTorch wheels - much smaller download than the default CUDA
# wheels, which matters on Streamlit Cloud's free tier (build size / RAM).
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit
opencv-python-headless==4.10.0.84
numpy
pandas
scikit-learn
joblib
seaborn
matplotlib
Pillow
tqdm
requests
torch
torchvision
ultralytics
